# Example environment variables for local development
# Copy this to .env and fill in your values
# DO NOT commit .env to version control

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=uddhava_db

# Optional settings
ENVIRONMENT=development
ALLOWED_ORIGINS=*
SQL_DEBUG=false
PORT=8000
WORKERS=1

# Debug token (uncomment to protect /debug/db)
# DEBUG_DB_TOKEN=some-long-random-string
