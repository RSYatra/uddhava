# Example environment variables for local development
# Copy this to .env and fill in your values
# DO NOT commit .env to version control

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=uddhava_db

# Optional settings
ENVIRONMENT=development
ALLOWED_ORIGINS=*
SQL_DEBUG=false
PORT=8000
WORKERS=1

# Debug token (uncomment to protect /debug/db)
# DEBUG_DB_TOKEN=some-long-random-string

# JWT Authentication Settings (REQUIRED for production)
JWT_SECRET_KEY=your-secret-key-change-this-in-production-use-openssl-rand-hex-32
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# Email Configuration (Required for password reset emails)
# Gmail Configuration (requires App Password - not your regular password!)
MAIL_USERNAME=radhashyamsundaryatra@gmail.com
MAIL_PASSWORD=trqq dnds umau pkhw
MAIL_FROM=radhashyamsundaryatra@gmail.com
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_STARTTLS=true
MAIL_SSL_TLS=false
MAIL_USE_CREDENTIALS=true
MAIL_VALIDATE_CERTS=true

# Frontend URL Configuration (IMPORTANT: Change these to your actual frontend domain)
# Replace https://yourfrontend.com with your actual frontend domain
EMAIL_VERIFICATION_URL_BASE=https://rsyatra.com/verify-email
PASSWORD_RESET_URL_BASE=https://rsyatra.com/reset-password
FRONTEND_LOGIN_URL=https://rsyatra.com/login
